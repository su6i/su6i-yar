# Core
python-telegram-bot[job-queue]>=20.0
python-dotenv>=1.0.0

# Video Download
yt-dlp>=2024.0.0
ffmpeg-python>=0.2.0

# AI / LangChain
langchain>=0.3.0
langchain-google-genai>=2.0.0
langchain-openai>=0.2.0

# TTS (Text-to-Speech)
edge-tts>=6.1.0
aiofiles>=23.0.0
# sherpa-onnx, onnx, numpy, soundfile removed for cleanup

# Scraping & Utilities
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Development Tools (for auto-restart)
watchdog>=4.0.0
# Note: nodemon is a Node.js package, if you prefer it, install via: npm install -g nodemon
